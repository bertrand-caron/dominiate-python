language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - cd dominiate && make coverage
  #- make pylint
after_success: cd dominiate && coveralls --ignore-errors
cache: pip
